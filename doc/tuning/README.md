# Speed-limit / Brake tuning store — **ARCHIVE** (1.16 Recommended era)

**Status:** Historical only. Recommended Limit adopt is **CUT** (2026-08-03 / **1.17**). Do not add new dial sessions for Limit adopt.

Kept for forensics on old Player.log / corpus rows (`rec30UnderHighPosted`, `suggest=`, `BrakeLimitAlign`, etc.). Soft **Brake** chip may still be tuned later via normal Tier 2 — not this dial store.

## Files

| File | Purpose |
|------|---------|
| [`knobs.jsonl`](knobs.jsonl) | Archive of 1.16 dial / constant ships |
| [`sessions.jsonl`](sessions.jsonl) | Archive smoke / log pass summaries |
| [`schema.md`](schema.md) | Field definitions (legacy) |
| `raw/*.jsonl` | Optional full `T2 limit` frame dumps (local only) |
